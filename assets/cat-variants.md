# Cat variants

Use this file to track cat visual experiments that may be restored later.

- `7c6b353` / current restored main cat: simpler CSS cat mark used before the fluffy experiment.
- `bb91c09`: fluffy main cat experiment with blush, paws, head fluff, and softer face styling. Reverted from the live UI because it felt less cute in context, but the code is preserved in git history.

For the next asset pass, use `assets/cat-character-sheet.md` as the source brief for consistent sprite states.
